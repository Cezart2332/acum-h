# Minimal requirements for AI system without ML
# Compatible with Python 3.13 and newer versions

# Core Web Framework
flask>=3.0.0
flask-cors>=4.0.0

# HTTP Requests
requests>=2.30.0

# Configuration
python-dotenv>=1.0.0

# JSON handling (optional but recommended)
ujson>=5.8.0

# Optional: If you want caching support
# redis>=5.0.0

# Optional: If you want basic ML features (may require compatible versions)
# numpy>=1.26.0
# scikit-learn>=1.3.0
# sentence-transformers>=2.2.2